[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "halo-reader"
version = "0.0.1"
authors = [
  { name="Niko Leskinen", email="niko.leskinen@fmi.fi" },
]
dependencies = [
  "numpy",
]
